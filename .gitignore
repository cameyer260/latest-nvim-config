# Local / generated artifacts
tags
spell/
.luarc.json

# Tool caches
.ruby-lsp/
.pi/

# OS
.DS_Store

# NOTE: nvim-pack-lock.json is intentionally tracked so plugin versions
# are reproducible (see `:help vim.pack-lockfile`).
